<?php
include"include.php";
rabota ("GET",$_GET);
rabota("POST",$POST);
if isset($_POST['name'])  ){
      if(
      strlen($_POST['name'] ) > 3 ) {

      }
      
      echo"valid";
      }else{
      echo"invalid";      }
 
   }
   }else{
   echo"no form data";
   }
            
       ?>
